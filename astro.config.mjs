// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // When you deploy, put your real address here. It is used for SEO tags.
  // Example: site: 'https://aarav.netlify.app',
});
